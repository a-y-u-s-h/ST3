<snippet>	
	<content><![CDATA[
# `rename` : "${1:original}" to "${2:newname}"
if os.path.exists(f"${1:original}") and not os.path.exists(f"${2:newname}"):
	os.rename(f"${1:original}", f"${2:newname}")

]]></content>
	<tabTrigger>rename</tabTrigger>
	<scope>source.python</scope>
	<description>rename</description>
</snippet>
