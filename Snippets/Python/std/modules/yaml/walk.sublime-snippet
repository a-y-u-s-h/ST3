<snippet> 
  <content><![CDATA[
with open(f"config.yaml", "r") as file:
  content = yaml.load(file)
  for key, item in content.items():
    if item is collection:
      walk(item)
    else:
      ${0}
]]></content>
  <tabTrigger>yamlwalk</tabTrigger>
  <scope>source.python</scope>
  <description>import</description>
</snippet>
