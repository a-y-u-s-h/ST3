<snippet>
	<content><![CDATA[
for (int ${6:d} = 0; ${6:d} < ${2:N} + ${3:M} - 1; ${6:d}++) {
  for (int ${4:i} = 0; ${4:i} < ${2:N}; ${4:i}++) {
    for (int ${5:j} = 0; ${5:j} < ${3:M}; ${5:j}++) {
      if (${4:i} + ${5:j} == ${6:d}) {
        $0
      }
    }
  }
}
]]></content>
	<tabTrigger>matrixdiagonaltraversaltopleftbottomright</tabTrigger>
	<scope>(source.c | source.objc | source.c++ | source.objc++) - meta.preprocessor.incomplete</scope>
</snippet>
