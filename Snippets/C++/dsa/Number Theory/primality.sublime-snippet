<snippet>
	<content><![CDATA[
bool primality (${1:long long} N) {
  if (N == 1) return false;
  for (${1:long long} i = 2; i <= std::sqrt(N); i++) { if (N % i == 0) return false; }
  return true;
}
]]></content>
	<tabTrigger>primality</tabTrigger>
	<scope>(source.c++ | source.objc++) - meta.preprocessor.incomplete</scope>
</snippet>
